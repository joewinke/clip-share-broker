{
  "name": "clip-share-broker",
  "version": "1.0.0",
  "description": "Turns an API key into a short-lived presigned MinIO/S3 upload URL — the extension and native host never hold the long-lived storage secret.",
  "type": "module",
  "private": true,
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  }
}
